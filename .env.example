# Discord
DISCORD_TOKEN=
CLIENT_ID=
GUILD_ID=

# Postgres (Prisma). Ejemplo para Railway:
# postgresql://usuario:password@host:puerto/nombre_bd
DATABASE_URL=

# Solo necesario para correr `npm run migrate:mysql` (migración one-time
# desde la base de datos MySQL anterior). No se usa en producción.
MYSQLHOST=
MYSQLUSER=
MYSQLPASSWORD=
MYSQLDATABASE=
MYSQLPORT=3306
